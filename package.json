{
  "name": "example-use-of-nuxt-contextual-transition",
  "description": "Example use of Nuxt Contextual Transition",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.4.0",
    "nuxt-contextual-transition": "^0.1.0"
  }
}
